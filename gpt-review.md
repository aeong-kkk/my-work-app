# GPT 검증 결과 — specs/sample-order-listing-flow.md

> 검증 기준: 사실 / 논리 / 누락 / 톤·형식. GPT(gpt-4o-mini)가 원본 문서를 기획서(sample-order-listing-plan.md)·기능1 스펙(feature-1-spec.md)·기능1 코드(feature1_extract_order.py)와 대조해 지적한 문제점만 정리한 결과이며, 문서 자체를 다시 쓴 것은 아닙니다.

## 사실
- **문장**: "구분(신규/재발주) | Info | F3 | 'NEW／Repeat'"
  **문제**: 문서에서는 구분을 "신규/재발주"로 설명하고 있으나, 실제 코드에서는 "NEW"와 "Repeat"로 처리되고 있어 일관성이 없다.

## 논리
- **문장**: "행 개수만큼 발주 건 생성 (발주일/구분/모델명/내부명/Total-T/층구성은 모든 행에 공통 적용)"
  **문제**: 이 문장은 발주 건 생성 시 구분이 모든 행에 공통 적용된다고 설명하지만, 실제로는 요청일이 서로 다른 경우 "분할입고"로 덮어쓰는 로직이 있어 이 설명이 부정확하다.

## 누락
- **문장**: "수량 값에서 숫자만 분리 (예: '16\n(IST Coupon)' → 16)"
  **문제**: 수량을 분리하는 과정에서 예시가 제공되었으나, 이 과정에서 발생할 수 있는 예외 상황(예: 수량이 비어있거나 잘못된 형식일 경우)에 대한 설명이 누락되어 있다.

## 톤·형식
- **문장**: "엑셀(xlsx) 리스트 파일 — 실행할 때마다 새로 만들지 않고 기존 파일에 누적(추가)"
  **문제**: "엑셀(xlsx) 리스트 파일"이라는 표현이 다소 어색하며, "엑셀 파일" 또는 "xlsx 파일"로 간단히 표현하는 것이 더 자연스럽다.
